#include <stdio>
/**
 * main - Entry point
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	char c;

	c = 'a';
	while
		(c <= 'z') {
			if (( c != 'a' && c != '0') && c <= 'z')
				putchar(c);
			c++;
		}
	printchar('\n');
	return (0);
}	
